# sekrety — NIGDY do repo
.env
.env.local
*.key

# dane surowe (cache odpowiedzi HTTP) — duże, odtwarzalne
data/raw/

# pliki towarzyszące WAL — transientne i niespójne bez bazy.
# Sama data/emscan.db JEST commitowana (SPEC §1.8) — po zamknięciu połączenia
# SQLite checkpointuje WAL, więc plik bazy jest kompletny.
data/emscan.db-wal
data/emscan.db-shm

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# narzędzia
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# modele (faza 2+) — wersjonujemy metadane, nie binaria
models/*.txt
models/*.pkl
models/*.lgb

# system
.DS_Store
